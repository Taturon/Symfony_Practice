controllers:
    resource: ../../src/Controller/
    type: annotation
    defaults:
        schemes: [https]
    prefix: '/blog'
    name_prefix: 'blog_'
    requirements:
        _locale: 'en|es|fr'

kernel:
    resource: ../../src/Kernel.php
    type: annotation
